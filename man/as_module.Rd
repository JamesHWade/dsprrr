% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsp.R
\name{as_module}
\alias{as_module}
\alias{as_module.Chat}
\alias{as_module.character}
\alias{as_module.dsprrr::Signature}
\title{Create a Module from a Chat}
\usage{
as_module(x, ...)

\method{as_module}{Chat}(x, signature, ...)

\method{as_module}{character}(x, ...)

\method{as_module}{`dsprrr::Signature`}(x, ...)
}
\arguments{
\item{x}{A Chat object, signature string, or Signature object.}

\item{...}{Additional arguments passed to \code{module()}.}

\item{signature}{When \code{x} is a Chat, the signature (string or Signature object).}
}
\value{
A Module object (R6) that wraps the Chat.
}
\description{
Creates a dsprrr Module that wraps an ellmer Chat object. The Module
can be used for repeated predictions, batch processing, and optimization.
}
\examples{
\dontrun{
# Create module from Chat
mod <- chat_openai() |> as_module("text -> sentiment")

# Use the module
mod$predict(text = "I love this!")

# Optimize
mod$optimize_grid(trainset, metric = metric_exact_match())

# Access underlying Chat
mod$chat$chat("Hello!")
}
}
