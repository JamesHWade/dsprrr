% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concurrency-control.R
\name{concurrency_control}
\alias{concurrency_control}
\title{Control Batch Concurrency}
\usage{
concurrency_control(
  backend = c("auto", "sequential", "ellmer", "mirai"),
  max_active = 1L,
  task_timeout = Inf,
  total_timeout = Inf,
  max_errors = Inf,
  cancel = TRUE
)
}
\arguments{
\item{backend}{Execution backend. \code{"auto"} uses sequential execution when
\code{max_active = 1}, otherwise preferring ellmer when the configured Chat and
requested limits are compatible, then mirai, then sequential execution.
Explicit \code{"ellmer"}, \code{"mirai"}, and \code{"sequential"} requests never fall
back.}

\item{max_active}{Positive integer giving the exact maximum number of active
requests. It maps to ellmer's \code{max_active} argument and the size of a
dsprrr-owned mirai pool.}

\item{task_timeout}{Per-task timeout in seconds, or \code{Inf} for no timeout.
Finite task timeouts currently require the mirai backend.}

\item{total_timeout}{Total batch execution timeout in seconds, or \code{Inf} for
no timeout. Finite total timeouts currently require the mirai backend.
Shutdown is initiated at the deadline and verification is bounded; the
native backend reset can add brief cleanup latency before \code{\link[=run]{run()}} returns.}

\item{max_errors}{Non-negative integer error budget, or \code{Inf}. Zero permits
work to begin and stops new work after the first failure. With ellmer, an
already-started wave of at most \code{max_active} rows completes before the
budget is observed.}

\item{cancel}{Logical. When \code{TRUE}, active mirai tasks are stopped when an
error or timeout limit is reached. When \code{FALSE}, no new work is scheduled,
but already-started tasks are drained before return. A total timeout always
stops active work so no task continues after \code{\link[=run]{run()}} returns.}
}
\value{
A \code{dsprrr_concurrency_control} object for \code{.concurrency} in \code{\link[=run]{run()}},
\code{\link[=run_dataset]{run_dataset()}}, or \code{\link[=evaluate]{evaluate()}}.
}
\description{
Create a validated execution policy for batch calls made by \code{\link[=run]{run()}} and
\code{\link[=run_dataset]{run_dataset()}}. The policy controls the backend, the exact maximum number
of active requests, error budgets, timeouts, and cancellation behavior.

\code{backend = "auto"} is the only mode that may select a different backend.
An explicitly requested backend either runs with the requested contract or
fails before provider work begins.
}
\details{
Native ellmer and mirai batch execution currently bypass dsprrr's
response cache; structured metadata reports this as \code{cache = "bypass"}.
Choose \code{backend = "sequential"} when response-cache reuse is required.
}
\examples{
control <- concurrency_control(
  backend = "mirai",
  max_active = 2L,
  task_timeout = 30,
  total_timeout = 120,
  max_errors = 1L
)
}
