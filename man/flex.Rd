% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module-flex.R
\name{flex}
\alias{flex}
\title{Experimental Declarative Flex Module}
\usage{
flex(
  signature,
  module_src = NULL,
  max_predictor_calls = 100L,
  config = list(),
  chat = NULL
)
}
\arguments{
\item{signature}{A \link{Signature} object or DSPy-style signature string.}

\item{module_src}{A version 1 Flex source as one JSON string, or \code{NULL} for
the single-predictor baseline.}

\item{max_predictor_calls}{Maximum number of predictor steps allowed in one
source.}

\item{config}{Optional module configuration passed to each fresh predictor.}

\item{chat}{Optional ellmer \code{Chat} used unless \code{.llm} is supplied at run
time.}
}
\value{
An experimental \code{FlexModule}.
}
\description{
\code{flex()} creates an experimental module whose predictor topology is itself an
optimization parameter. The topology is supplied as canonical JSON in
\code{module_src}; it is parsed as data and is never evaluated as R code.

Version 1 sources contain \code{schema_version}, an ordered \code{steps} array, and an
\code{outputs} object. Each step has a safe, unique \code{name}, a \code{primitive} of
\code{"predict"} or \code{"chain_of_thought"}, a \code{signature} (\code{"$outer"} or DSPy
string notation), an optional \code{instructions} string, and an \code{inputs} object.
Input references use \code{"$input.<name>"} or
\code{"$step.<earlier-step>.<field>"}. \code{outputs} maps every outer output field to
one of the same reference forms. Sources are type checked before binding.
Flex v1 supports string, number, integer, boolean, enum, array, and non-empty
object signature types. Opaque \code{TypeJsonSchema} values and empty objects are
rejected because their interfaces cannot be checked safely by this compiler.

When \code{module_src} is \code{NULL}, the baseline is one \code{predict} step over the
outer signature. \verb{$bind()} and \verb{$apply_optimization_params()} validate a new
source transactionally, so an invalid candidate cannot replace the active
plan. The canonical source is available through the read-only
\verb{$module_src} active binding.

GEPA optimizes Flex programs as complete instruction and \code{module_src}
component snapshots. Invalid source candidates are scored for audit but are
not selectable. GEPA-lite ranks whole-program candidates; it does not
implement DSPy's independent per-component Pareto frontiers or inference-time
search.

\code{\link[=run_async]{run_async()}}, \code{\link[=stream_async]{stream_async()}}, and a module's \verb{$stream()} method reject Flex
because their direct provider path would bypass its graph. The \code{\link[=run_stream]{run_stream()}}
one-shot \code{forward()} fallback remains available, but an actual token-stream
request is rejected before provider work.
}
\examples{
program <- flex("question -> answer")
program$module_src

\dontrun{
result <- run(program, question = "Why is the sky blue?", .llm = llm)
}
}
