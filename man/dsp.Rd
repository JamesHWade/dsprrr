% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsp.R
\name{dsp}
\alias{dsp}
\alias{dsp.Chat}
\alias{dsp.character}
\alias{dsp.dsprrr::Signature}
\title{Declarative Structured Prediction}
\usage{
dsp(x, ...)

\method{dsp}{Chat}(x, signature, ..., .instructions = NULL, .echo = "none", .simplify = TRUE)

\method{dsp}{character}(x, ..., .instructions = NULL, .echo = "none", .simplify = TRUE)

\method{dsp}{`dsprrr::Signature`}(x, ..., .instructions = NULL, .echo = "none", .simplify = TRUE)
}
\arguments{
\item{x}{A Chat object, signature string, or Signature object.}

\item{...}{Named arguments matching the signature's inputs.}

\item{signature}{When \code{x} is a Chat, the signature (string or Signature object).}

\item{.instructions}{Optional additional instructions (appended to signature instructions).}

\item{.echo}{Whether to echo output. See \link[ellmer:Chat]{ellmer::Chat} for options.}

\item{.simplify}{Logical. If \code{TRUE} (default), single-field outputs are
simplified to just the value. If \code{FALSE}, always returns a named list.}
}
\value{
The structured output according to the signature's output type.
When \code{.simplify = TRUE} (default): single-field outputs return just the
value, multi-field outputs return a named list.
When \code{.simplify = FALSE}: always returns a named list.
}
\description{
The \code{dsp()} function provides a pipe-friendly interface for structured
LLM predictions using dsprrr signatures. It's designed to feel familiar
to ellmer users while providing the power of declarative signatures.
}
\details{
\code{dsp()} is the simplest way to use dsprrr. It takes a Chat object and a
signature, builds a prompt from the provided inputs, and returns the
structured output. For more control (optimization, tracing), use
\code{as_module()} to create a reusable Module.

The function dispatches on the first argument:
\itemize{
\item \code{dsp.Chat}: When piping from an ellmer Chat object
\item \code{dsp.character}: When providing a signature string (uses default Chat)
\item \code{dsp.dsprrr::Signature}: When providing a Signature object
}
}
\examples{
\dontrun{
# Pipe-friendly usage with Chat
chat <- ellmer::chat_openai()
chat |> dsp("question -> answer", question = "What is 2+2?")

# With complex output types
chat |> dsp(
  "text -> sentiment: enum('positive', 'negative', 'neutral')",
  text = "I love this package!"
)

# Using default Chat (auto-detected from env vars)
dsp("question -> answer", question = "What is the capital of France?")

# With additional instructions
chat |> dsp(
  "text -> summary",
  text = long_article,
  .instructions = "Keep the summary under 50 words"
)

# Control output simplification
dsp("q -> answer", q = "Hi", .simplify = TRUE)   # Returns: "Hello"
dsp("q -> answer", q = "Hi", .simplify = FALSE)  # Returns: list(answer = "Hello")
}
}
