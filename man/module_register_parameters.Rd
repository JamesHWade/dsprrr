% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize.R
\name{module_register_parameters}
\alias{module_register_parameters}
\title{Register module parameter metadata}
\usage{
module_register_parameters(module, parameters)
}
\arguments{
\item{module}{A DSPrrr module object.}

\item{parameters}{Named list where each element is a list containing optional
fields: \code{type} (\code{"double"}, \code{"integer"}, \code{"categorical"}, \code{"logical"}),
\code{range} (numeric length-two vector), \code{values} (vector of allowed values),
\code{label} (human-readable description), \code{inclusive} (length-two logical
vector), and \code{trans} (tidymodels transformation object).}
}
\value{
The modified module (invisibly).
}
\description{
Store parameter metadata on a module so that \code{module_parameter_set()} can
construct tidymodels parameter sets without relying solely on observed values.
Each entry describes the parameter type plus either a range (for quantitative
parameters) or explicit values (for qualitative parameters).
}
\examples{
\dontrun{
mod <- signature("text -> sentiment") |> module(type = "predict")
module_register_parameters(mod, list(
  temperature = list(type = "double", range = c(0, 1)),
  prompt_style = list(type = "categorical", values = c("baseline", "energetic"))
))
module_parameter_set(mod)
}
}
