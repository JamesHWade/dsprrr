% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assertion.R
\name{assertions}
\alias{assertions}
\alias{Assertion}
\alias{AssertionSet}
\alias{assert_output}
\alias{suggest_output}
\alias{assertion_set}
\title{Assertions for Output Validation}
\usage{
Assertion(
  condition = function() NULL,
  message = "Assertion failed",
  field = NULL,
  type = "assert"
)

AssertionSet(assertions = list())

assert_output(condition, message = "Assertion failed", field = NULL)

suggest_output(condition, message = "Suggestion not met", field = NULL)

assertion_set(...)
}
\arguments{
\item{condition}{A formula or function that takes the output and returns TRUE/FALSE.
For formulas, use \code{.x} to reference the output (e.g., \code{~ nchar(.x$answer) <= 100}).}

\item{message}{Error message to display when the assertion fails.}

\item{field}{Optional. The specific output field to validate. If NULL, the entire
output is passed to the condition.}

\item{type}{For \code{Assertion} class: "assert" for hard assertion, "suggest" for soft
suggestion. Use the helper functions \code{assert_output()} and \code{suggest_output()}
instead of setting this directly.}

\item{assertions}{For \code{AssertionSet} class: A list of Assertion objects.}

\item{...}{Assertion objects or a list of Assertion objects.}
}
\value{
An Assertion object.

An AssertionSet object.
}
\description{
S7 classes and helper functions for defining output validation constraints
with backtracking support. Hard assertions trigger retries when they fail,
while soft suggestions log warnings but allow execution to continue.

Define validation constraints for module outputs. Hard assertions (\code{assert_output})
trigger retries when they fail, while soft suggestions (\code{suggest_output}) log
warnings but allow execution to continue.
}
\details{
\subsection{Backtracking Behavior}{

When wrapped with \code{with_assertions()}, modules will:
\enumerate{
\item Run the module normally
\item Evaluate all assertions against the output
\item If hard assertions fail and retries remain, inject feedback and retry
\item If max retries exceeded, raise an error (or warning if configured)
\item Soft suggestions always log but never trigger retries
}
}

\subsection{Condition Functions}{

Conditions can be specified as:
\itemize{
\item \strong{Formulas}: \code{~ nchar(.x$answer) <= 100} - \code{.x} is the output
\item \strong{Functions}: \code{function(x) nchar(x$answer) <= 100}
}
}
}
\examples{
\dontrun{
# Hard assertion - must be satisfied
assert_output(~ nchar(.x$answer) <= 100, "Answer must be 100 chars or less")

# Soft suggestion - logs warning but continues
suggest_output(~ grepl("^[A-Z]", .x$answer), "Should start with capital")

# Field-specific assertion
assert_output(~ nchar(.x) <= 50, "Too long", field = "summary")
}

\dontrun{
assertions <- assertion_set(
  assert_output(~ nchar(.x$answer) <= 100, "Too long"),
  suggest_output(~ grepl("^[A-Z]", .x$answer), "Should capitalize")
)
}

}
