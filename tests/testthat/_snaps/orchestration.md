# restore_module_config rejects legacy pinned configs

    Code
      restore_module_config(legacy_config)
    Condition
      Error in `malformed()`:
      ! Malformed dsprrr program artifact
      x Artifact must be a list.
